- 吴海林

20+15+15+10+10=70
使用了结构体挺好的，还知道传入句柄控制LED，GOOD，但是你的初始化函数参数太多了，你可以先初始化一个结构体再传入LED_Init()函数就更好了，同样没有判断是否是空指针

命名规范注意一下，初始化函数应该是LED_init()，函数不能全部大写，命名也可以改改，ActiveLevel改为state，CONTORL_form改为mode，用常见的词汇来替换


## 总结
---

1. 命名要规范。
2. 要有防御措施，防止程序奔溃。
3. 函数传入参数不要太多，多拆分成一个个小函数。
